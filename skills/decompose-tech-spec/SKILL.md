---
description: Decompose approved tech-spec into tasks with validation (delegates via Codex shim)
---

# Instructions

Resolve and execute the target skill via shim:

```powershell
pwsh -File shared/scripts/dispatch-skill.ps1 -SkillAlias decompose-tech-spec -AsPrompt
```