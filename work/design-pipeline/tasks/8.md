---
status: planned
depends_on: [3, 4, 5, 6]
wave: 3
skills: [code-writing]
verify: [smoke]
reviewers: [code-reviewer, security-auditor, test-reviewer]
teammate_name:
estimated_loc: 10
---

# Task 8: Delete like-figma skill

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Remove the `skills/like-figma/` directory — the original monolithic design skill that has been fully migrated into 4 new focused skills (design-system-init, design-generate, design-review, design-retrospective) and shared references (shared/design-references/).

This is the final cleanup task in the design-pipeline feature. All references, assets, and logic from like-figma have been copied to their new locations by Tasks 1-6. Per Decision 6 (copy, not symlink — delete originals after migration verified), deletion happens only after all 4 new skills are confirmed to exist and contain the migrated content.

Risk mitigation: before deletion, verify that every new skill directory and shared/design-references/ exist and are populated. If any verification fails, abort deletion and report what is missing.

## What to do

1. **Verify migration completeness** — confirm all 4 new skill directories exist and contain SKILL.md:
   - `skills/design-system-init/SKILL.md`
   - `skills/design-generate/SKILL.md`
   - `skills/design-review/SKILL.md`
   - `skills/design-retrospective/SKILL.md`

2. **Verify shared references exist** — confirm `shared/design-references/` contains:
   - `design-tokens.md`
   - `color-psychology.md`
   - `color-principles.md`
   - `library-catalog.md`

3. **Verify migrated assets exist** in design-generate:
   - `skills/design-generate/references/component-patterns.md`
   - `skills/design-generate/references/generation-guide.md`
   - `skills/design-generate/references/grid-techniques.md`
   - `skills/design-generate/assets/preview-template.html`

4. **Check for remaining references to like-figma** — grep the codebase for `like-figma` paths. If any skill or config still references `skills/like-figma/`, report and fix before deleting.

5. **Delete `skills/like-figma/` directory** entirely (all files and subdirectories).

6. **Final verification** — confirm `skills/like-figma/` no longer exists.

## Acceptance Criteria

- [ ] `skills/like-figma/` directory no longer exists
- [ ] All 4 new skill directories exist with SKILL.md: design-system-init, design-generate, design-review, design-retrospective
- [ ] `shared/design-references/` exists with design-tokens.md, color-psychology.md, color-principles.md, library-catalog.md
- [ ] `skills/design-generate/references/` contains component-patterns.md, generation-guide.md, grid-techniques.md
- [ ] `skills/design-generate/assets/preview-template.html` exists
- [ ] No remaining references to `skills/like-figma/` in the codebase
- [ ] No regression — all new skills remain intact after deletion

## Context Files

**Feature:**
- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)

**Project knowledge:**
- [project.md](../../../shared/templates/new-project/.agents/skills/project-knowledge/references/project.md)
- [architecture.md](../../../shared/templates/new-project/.agents/skills/project-knowledge/references/architecture.md)

**Code files to read (verify existence before deletion):**
- [skills/design-system-init/SKILL.md](../../../skills/design-system-init/SKILL.md)
- [skills/design-generate/SKILL.md](../../../skills/design-generate/SKILL.md)
- [skills/design-review/SKILL.md](../../../skills/design-review/SKILL.md)
- [skills/design-retrospective/SKILL.md](../../../skills/design-retrospective/SKILL.md)
- [shared/design-references/](../../../shared/design-references/)

**Code files to modify (delete):**
- `skills/like-figma/` (entire directory)

## Verification Steps

### Smoke
- Verify like-figma directory no longer exists: `test ! -d skills/like-figma && echo "PASS: like-figma deleted" || echo "FAIL: like-figma still exists"`
- Verify all 4 new skills exist:
  ```
  for skill in design-system-init design-generate design-review design-retrospective; do
    test -f "skills/$skill/SKILL.md" && echo "PASS: $skill" || echo "FAIL: $skill missing"
  done
  ```
- Verify shared references exist:
  ```
  for ref in design-tokens.md color-psychology.md color-principles.md library-catalog.md; do
    test -f "shared/design-references/$ref" && echo "PASS: $ref" || echo "FAIL: $ref missing"
  done
  ```
- Verify no remaining references to like-figma: `grep -r "like-figma" skills/ shared/ --include="*.md" | grep -v "decisions.md" | grep -v "tech-spec.md" | grep -v "user-spec.md" | grep -v "tasks/" && echo "FAIL: stale references found" || echo "PASS: no stale references"`

## Details

**Files:**
- `skills/like-figma/` — delete entire directory. This contained: SKILL.md, references/ (design-tokens.md, component-patterns.md, generation-guide.md), assets/ (preview-template.html). All content has been migrated by Tasks 1-6.

**Dependencies:**
- Task 1 (shared design-references) — migrated design-tokens.md, color-psychology.md, color-principles.md
- Task 3 (design-system-init) — migrated like-figma Phase 1-2 logic
- Task 4 (design-generate) — migrated like-figma Phase 3 logic, component-patterns.md, generation-guide.md, preview-template.html, grid-techniques.md
- Task 5 (design-review) — new skill, no direct migration from like-figma
- Task 6 (design-retrospective) — new skill, no direct migration from like-figma
- Decision 6: "Copy, not symlink. Delete originals after migration verified."

**Edge cases:**
- like-figma directory does not exist (already deleted or never existed in this branch) — treat as success, verify new skills still exist
- Remaining references to `skills/like-figma/` in other files (e.g., AGENTS.md, other SKILL.md files) — must be found and updated/removed before deletion
- Git history preserves like-figma content — deletion is only from working tree, history is intact

**Implementation hints:**
- Run `grep -r "like-figma"` across the repo before deletion to catch any stale references
- Use `rm -rf skills/like-figma` for the actual deletion
- The grep for stale references should exclude spec files (tech-spec.md, user-spec.md, decisions.md, task files) which legitimately mention like-figma as historical context

## Reviewers

- **code-reviewer** → `logs/working/task-8/code-reviewer-{round}.json`
- **security-auditor** → `logs/working/task-8/security-auditor-{round}.json`
- **test-reviewer** → `logs/working/task-8/test-reviewer-{round}.json`

## Post-completion

- [ ] Записать краткий отчёт в decisions.md по шаблону (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
